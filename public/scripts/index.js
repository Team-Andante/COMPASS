import { nextPage } from "./toast.js";

const scholarship = document.querySelector('a');

if (scholarship) {
    scholarship.addEventListener('click', (e) => {
        console.log("buttom");
    });
}